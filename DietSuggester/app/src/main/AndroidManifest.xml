<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dietsuggester">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DietSuggester">
        <activity android:name=".TipsActivity"></activity>
        <activity android:name=".ProteinActivity" />
        <activity android:name=".CarbohydrateActivity" />
        <activity android:name=".ChooseActivity" />
        <activity android:name=".DisplayActivity" />
        <activity android:name=".login" />
        <activity android:name=".Diet"/>


        <activity android:name=".MainActivity"/>
        <activity android:name=".register">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FoodActivity" />
    </application>

</manifest>